둘다 자바에서 관계형 데이터베이스에 접근하기 쉽도록 해주는 API를 가진것들인데,
jdbc보다도 mybatis 프레임워크가 좀 더 쉽고 관리하기 편하게 돼있다.

jdbc는 코드마다 sql문이 들어가야 하기때문에 쿼리를 수정하려면 일일이 자바 코드를 수정해줘야 하지만
mybatis를 사용하면 xml에 있는 쿼리문만 수정하면 되기 때문에 매우 간편하다

mybatis는 DAO객체의 인터페이스인 IDAO를 맵퍼로 갖는다.
우리는 xml파일에서 IDAO에 쿼리문만 맵핑 시켜주면 나머지는 mybatis가 다 해준다
(디비 접근, 값 가져오기, 업데이트하기 등등)
